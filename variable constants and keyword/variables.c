#include <stdio.h>
#include <conio.h>

int main()
{
    int a = 3;
    // int b = 4.5; // not recomented because 4.5 is not an integer
    float b = 8.5;
    char c = 'u';
    printf("the value of a is %d \n b is %f \n c is %c", a, b, c);
}