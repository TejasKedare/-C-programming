// Write an program to calculate the area of rectangle
#include <stdio.h>
int main()
{
    int length, width;

    printf("enter the length  ");
    scanf("%d", &length);
    printf("enter the width  ");
    scanf("%d", &width);
    printf("The area of the rectangle is %d", length * width);
}